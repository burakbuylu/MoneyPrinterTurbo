x-common-volumes: &common-volumes
  - ./:/MoneyPrinterTurbo

services:
  webui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "moneyprinterturbo-webui"
    ports:
      - "127.0.0.1:8501:8501"
    command: [ "streamlit", "run", "./webui/Main.py","--browser.serverAddress=127.0.0.1","--server.enableCORS=True","--browser.gatherUsageStats=False" ]
    volumes: *common-volumes
    restart: always
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "moneyprinterturbo-api"
    ports:
      - "127.0.0.1:8080:8080"
    command: [ "python3", "main.py" ]
    volumes: *common-volumes
    restart: always
  autopilot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "moneyprinterturbo-autopilot"
    # X saatte bir titles.txt'den video uretip YouTube'a yukler.
    # Etkinlestirmek icin config.toml'da autopilot_enabled = true yapin.
    command: [ "python3", "-m", "app.autopilot_worker" ]
    volumes: *common-volumes
    restart: always
